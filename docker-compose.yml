version:  '3.0'

services:
  front:
    build:
      context: .
    env_file:
      - .development.env
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5001:5001"
    command: yarn run start
    restart: always
