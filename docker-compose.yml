version:  '3.0'

services:
  front:
    container_name: front
    build:
      context: .
    env_file:
      - .development.env
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5001:5001"
    command:
      - npm run start
    restart: always
